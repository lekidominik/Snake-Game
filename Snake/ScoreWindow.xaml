<Window x:Class="Snake.ScoreWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Scores"
        Height="420"
        Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundColor}"
        FontFamily="{StaticResource MainFont}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="HIGH SCORES"
                   Foreground="{StaticResource TextColour}"
                   FontSize="26"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,18"/>

        <DataGrid x:Name="ScoresDataGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserResizeRows="False"
                  SelectionMode="Single"
                  HeadersVisibility="Column"
                  Background="{StaticResource GridBackgroundColor}"
                  Foreground="{StaticResource TextColour}"
                  RowBackground="{StaticResource GridBackgroundColor}"
                  AlternatingRowBackground="{StaticResource BackgroundColor}"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="{StaticResource GridLineColor}"
                  BorderBrush="{StaticResource GridLineColor}"
                  BorderThickness="2"
                  FontSize="14">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding Name}"
                                    Width="2*"/>

                <DataGridTextColumn Header="Score"
                                    Binding="{Binding Score}"
                                    Width="*"/>

                <DataGridTextColumn Header="Date"
                                    Binding="{Binding Date, StringFormat=yyyy.MM.dd HH:mm}"
                                    Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2" Margin="0,18,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="Delete Selected"
                    Width="150"
                    Height="36"
                    HorizontalAlignment="Left"
                    Background="{StaticResource GridLineColor}"
                    Foreground="{StaticResource TextColour}"
                    Click="DeleteButton_Click"/>

            <Button Grid.Column="1"
                    Content="Close"
                    Width="100"
                    Height="36"
                    HorizontalAlignment="Right"
                    Background="{StaticResource GridLineColor}"
                    Foreground="{StaticResource TextColour}"
                    Click="CloseButton_Click"/>
        </Grid>
    </Grid>
</Window>